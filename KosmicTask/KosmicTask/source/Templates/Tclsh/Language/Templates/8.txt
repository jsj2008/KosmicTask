#
# Created by Mugginsoft on 27 Mar 2012 14:36:41
#
# Credits:
#
# Notes: 
#
# the number of task arguments is:
# $argc
#
# task arguments are accessed as:
# [lindex $argv 0] .. [lindex $argv [expr $argc - 1]]
# 

proc runTask {} {

	set files ""
	
	# define command
	set command "screencapture"
		
	for { set i 1 } { $i <= 5 } { incr i } {
		
		if {$i > 1} {
			append files ", "
		}
		
		# file created in task current directory will be automatically deleted
		set file "capture-$i.png"
	 
	 	# log command
		puts stderr "Running $command command..."
	
		# execute command
		if {[catch {exec $command -t png $file}]} {
			error "Error running $command"
		}
	
		append files "$file"
		
		# log wait
		puts stderr "Waiting..."
	
		# sleep 1 second
		after 1000
	}
	
	# return a YAML inline format dictionary with a properly escaped array of filenames
	set result "{kosmicFile: \[$files] , kosmicInfo: files returned}"
	
	return $result
}

# run task function and catch errors
if {[catch runTask result] == 1} {
	set result "{kosmicError: $errorInfo}"	
}

# print result
puts "---"
puts $result
