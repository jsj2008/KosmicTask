#
# Created by {{ author }} on {% now | date_format: "dd MMM yyyy HH:mm:ss" %}
#
# Credits:
#
# Notes: 
#
# the number of task arguments is:
# $numArgs = $#ARGV + 1;
#
# task arguments are accessed as:
# $ARGV[0] .. $ARGV[$numArgs]
# 

use warnings;

# an inline array
$items = "[item 1, item 2]";

# css
$css = "color:green;font-weight:bold;font-style:italic;text-decoration:underline;font-size:18pt;";

# return a YAML inline format dictionary with array as data
$result = "--- {kosmicData: $items, kosmicStyle: '$css'}";

print $result;